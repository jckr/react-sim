---
Title: Radio
---

import {
  RadioExample,
} from '../../components/examples/controls-examples';
import { RadioAPI } from '../../components/api';

# Radio

A radio control will create several radio buttons, one of which is selected.

<RadioExample />

`radio` takes:

<RadioAPI />

```jsx
<Model
  showTimer={false}
  initialParams={{ options: 'jet' }}
  controls={[
    {
      type: 'radio',
      label: 'Choose one',
      param: 'options',
      options: ['jet', 'set', 'radio'],
    },
  ]}
/>
```
