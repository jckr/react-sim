---
Title: Checkbox
---

import {
  CheckBoxExample,
} from '../../components/examples/controls-examples';

# Checkbox

<CheckBoxExample />

A Checkbox is a simple control that will cause a param to toggle from true to false.
It doesn't need anything else besides the common props.

```jsx
<Model
  showTimer={false}
  initialParams={{ checked: false }}
  controls={{
    type: 'checkbox',
    label: 'Check me',
    param: 'checked',
  }}
/>
```

