---
Title: Installation
---

`react-sim` is on [npm](https://www.npmjs.com/package/react-sim). To install the latest stable version, run the following command:

```
npm install react-sim
```

Or, if you are using [yarn](https://classic.yarnpkg.com/en/):

```
yarn add react-sim
```


